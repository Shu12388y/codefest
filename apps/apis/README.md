```
npm install
npm run dev
```

Set `CORS_ORIGINS` in the API `.env` file as a comma-separated list of allowed frontend URLs, for example:

```env
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
```

```
open http://localhost:3000
```
